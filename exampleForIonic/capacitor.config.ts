import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fumi.iamport',
  appName: 'exampleForIonic',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
